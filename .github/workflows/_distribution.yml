name: Main Extension Distribution Pipeline
on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' || github.sha }}

jobs:
  #  duckdb-stable-build:
  #  name: Build extension binaries
  #  uses: jaicewizard/extension-ci-tools/.github/workflows/_extension_distribution.yml@fix_ccache
  #  with:
  #    duckdb_version: v1.1.0
  #    extension_name: faiss
  #    vcpkg_url: "https://github.com/jaicewizard/vcpkg.git"
  #    vcpkg_commit: bb9a822e61481d93df028e3075b1c2118f09b9a4
  #    exclude_archs: "wasm_mvp;wasm_eh;wasm_threads;windows_amd64"
  #    build_duckdb_shell: false

  windows:
    name: Windows
    runs-on: windows-latest
    env:
      BUILD_SHELL: '0'
      CC: 'gcc'
      CXX: 'g++'

    steps:
      - name: Keep \n line endings
        shell: bash
        run: |
          which gcc
          which g++
          git config --global core.autocrlf false
          git config --global core.eol lf

      - uses: actions/checkout@v3
        name: Checkout current repository
        with:
          fetch-depth: 0
          submodules: 'true'

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: 'devel'
          update-rtools: true
          rtools-version: '42' # linker bug in 43

      - name: Initialise rtools SSL certs
        shell: bash
        run: |
          bash -lc "echo"

      - name: Setup Ccache
        uses: hendrikmuhs/ccache-action@main
        continue-on-error: true
        with:
          key: ${{ github.job }}-windows_amd64_rtools

      - name: Cache Primes
        id: cache-primes
        uses: actions/cache@v4
        with:
          path: .ccache
          key: ${{ runner.os }}-2ksajns

      - name: Generate Prime Numbers
        shell: bash
        run: |
          make release 
